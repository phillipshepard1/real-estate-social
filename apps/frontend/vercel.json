{
  "buildCommand": "cd ../.. && pnpm install && pnpm build:frontend",
  "outputDirectory": "apps/frontend/.next",
  "installCommand": "cd ../.. && pnpm install",
  "framework": "nextjs",
  "regions": ["iad1"],
  "functions": {
    "app/**/*": {
      "maxDuration": 10
    }
  },
  "env": {
    "NODE_ENV": "production"
  }
}
